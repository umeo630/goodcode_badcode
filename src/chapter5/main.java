public class main {
    /**
     * 様々な用途で使用されてしまう
     */
    // 標準会員の入会ポイント
    GiftPoint standardMemberShipPoint = new GiftPoint(100);

    // プレミアム会員の入会ポインｔ
    GiftPoint premiumMemberShipPoint = new GiftPoint(300);
}
